config:
  target: "http://localhost:3000"
  phases:
    - duration: 10
      arrivalRate: 20
scenarios:
  - flow:
      - get:
          url: "/"
      - emit:
          channel: "CLIENT_LOGIN"
          data: "ping"
          acknowledge:
            match:
              json: "$.1.answer"
              value: 42
